## Packages
framer-motion | For smooth page transitions and UI animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
The application logic for Chronophotography (video processing, frame extraction, canvas manipulation) will be handled primarily on the client side using the HTML5 Canvas API and Video API.
Dark mode is the default and primary theme.
